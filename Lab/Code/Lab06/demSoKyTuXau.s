	AREA RESET, DATA, READONLY
		DCD 0X20000000
		DCD MAIN

CR EQU 0X00	; CR LA KI TU KET THUC
XAU DCB "VAYHA", CR; xau ket thuc voi ki tu null

	AREA MYCODE, CODE, READONLY
		ENTRY

MAIN
	LDR R0, = XAU
	MOV R1, #0; DEM SO KI TU

LOOP
	LDRB R2, [R0], #1; LAY KI TU RA ; VI LA DCB: 1 BYTE NEN LAY LDR THEO BYTE-> LDRB
	CMP R2, #CR
	BEQ STOP
	ADD R1, #1
	B LOOP

STOP
	B STOP
	END

